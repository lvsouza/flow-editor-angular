import { AppDraggableDirective } from './app-draggable.directive';

describe('AppDraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new AppDraggableDirective();
    expect(directive).toBeTruthy();
  });
});
