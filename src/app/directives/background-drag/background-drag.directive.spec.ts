import { BackgroundDragDirective } from './background-drag.directive';

describe('BackgroundDragDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundDragDirective();
    expect(directive).toBeTruthy();
  });
});
